/**
 * Steg 2: Vi lager en virtuell dartskive som kan sende meldinger
 * (THROW_DETECTED, TAKEOUT_STARTED/FINISHED) mot serveren.
 * I første omgang: CLI / programmatisk kontroll. Senere: Admin-UI knapper.
 */
console.log("Simulator placeholder – implementeres i Steg 2.");
